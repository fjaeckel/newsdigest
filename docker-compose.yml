services:
  newsdigest:
    build: .
    image: newsdigest:latest
    container_name: newsdigest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
